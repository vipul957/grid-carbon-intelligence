[project]
name = "grid_carbon_intelligence"
version = "0.1.0"
description = "Carbon-aware workload scheduling and grid-intensity analytics for efficient computing."
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "pandas>=2.0", "scikit-learn>=1.3"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
